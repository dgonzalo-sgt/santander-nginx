apiVersion: v1
kind: Service
metadata:
  name: {{ include "santander-nginx.fullname" . }}
  labels:
    {{- include "santander-nginx.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "santander-nginx.selectorLabels" . | nindent 4 }}
